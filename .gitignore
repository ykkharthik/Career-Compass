out/
*.class
server.log
data/mail.properties
# H2 database files (NotificationRepository) - regenerated fresh on first
# run, same reason a real app never commits its live database to git.
*.mv.db
*.trace.db
# Presentation prep, not part of the codebase itself
presentation_script_*.md
